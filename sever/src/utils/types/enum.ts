export enum EStatus {
    PUBLIC = "public",
    PRIVATE = "private"
}

export enum EUserStatus {
    BANNED = "banned",
    ACTIVE = "active",
    PENDING = "pending"
}

export enum EContactStatus {
    PENDING = "pending",
    RESOLVED = "resolved",
}