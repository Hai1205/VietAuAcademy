export enum EStatus {
    DELETED = "deleted",
    ACTIVE = "active"
}

export enum EUserStatus {
    BANNED = "banned",
    ACTIVE = "active",
    PENDING = "pending"
}

export enum EContactStatus {
    PENDING = "pending",
    RESOLVED = "resolved",
}