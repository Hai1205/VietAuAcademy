export enum ETheme {
    LIGHT = "light",
    DARK = "dark",
}

export enum EStatus {
    DELETED = "deleted",
    ACTIVE = "active"
}

export enum EUserStatus {
    PENDING = "pending",
    BANNED = "banned",
    ACTIVE = "active"
}

export enum EContactStatus {
    PENDING = "pending",
    RESOLVED = "resolved",
}