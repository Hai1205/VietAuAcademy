export enum EStatus {
    PUBLIC = "public",
    PRIVATE = "private"
}

export enum EUserStatus {
    PENDING = "pending",
    BANNED = "banned",
    ACTIVE = "active"
}

export enum EContactStatus {
    PENDING = "pending",
    RESOLVED = "resolved",
}